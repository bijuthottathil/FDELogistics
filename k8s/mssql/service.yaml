apiVersion: v1
kind: Service
metadata:
  name: mssql
spec:
  selector:
    app: mssql
  ports:
    - port: 1433
      targetPort: 1433
  # ClusterIP: only the app and init Jobs need to reach it, never exposed
  # outside the cluster.
